# Toolkit implementation area

Place exported/verified Toolkit data here after recreating the class inside BG3 Toolkit. The hand-authored Stats drafts live under `src/stats/`; this directory is reserved for data that has actually been generated or validated in the editor.
